<yassersouri.github.io>

## Adding a blog post

1. Copy an existing post in `blog/posts/` to a new file named `YYYY-MM-DD-slug.html`.
2. Update the `<title>`, `<meta>` tags, `<h1>`, date subtitle, and body content.
3. Add an entry for the post at the top of the list in `blog/index.html`.
